spring:
  application:
    name: MS-Personal
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/muebleStgo_Personal
    username: root
    password: Stark021
  jpa:
    hibernate:
      ddl-auto: update
